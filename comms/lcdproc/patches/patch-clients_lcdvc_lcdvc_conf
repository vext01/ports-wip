$OpenBSD$

OpenBSD path and there is no point in logging to stderr if we use daemon() to daemonise
(stderr will get closed).

--- clients/lcdvc/lcdvc.conf.orig	Tue May 19 07:44:47 2009
+++ clients/lcdvc/lcdvc.conf	Tue Nov 15 17:06:51 2011
@@ -10,7 +10,7 @@
 #Port=13666
 
 #If false, report to stderr. If true, to syslog.
-#ReportToSyslog=true
+ReportToSyslog=true
 
 # Same as with LCDd.
 #ReportLevel=4
@@ -19,7 +19,7 @@
 #Foreground=false
 
 # PidFile location when running as daemon [default: /var/run/lcdvc.pid]
-#PidFile=/var/run/lcdvc.pid
+PidFile=/var/run/lcdproc/lcdvc.pid
 
 # Keys to move the visible area around the screen.
 #UpKey=Up
