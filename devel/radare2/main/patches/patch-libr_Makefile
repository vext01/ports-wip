$OpenBSD$

No library symlinks

--- libr/Makefile.orig	Sat Jul 16 01:30:33 2011
+++ libr/Makefile	Sat Jul 16 02:02:23 2011
@@ -63,7 +53,6 @@ install: install-includes install-pkgconfig
 	  b=`echo $$a|cut -d / -f 2`; \
 	  echo " ${PFX}/lib/$$b.${LIBVERSION}"; \
 	  ${INSTALL_LIB} $$a ${PFX}/lib/$$b.${LIBVERSION} ; \
-	  ( cd ${PFX}/lib ; ln -fs $$b.${LIBVERSION} $$b.0 ; ln -fs $$b.0 $$b ); \
 	done
 	# object archives
 	@for a in `find * -type f | grep -e '\.a$$' | grep -v fs/p` ; do \
